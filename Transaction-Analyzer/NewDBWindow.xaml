<Window x:Class="Transaction_Analyzer.NewDBWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Transaction_Analyzer"
        mc:Ignorable="d"
        Title="NewDBWindow" Height="300" Width="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Label Margin="0,0,0,3"
                   Grid.Row="1"
                   Grid.Column="0">Database Name</Label>
        <TextBox x:Name="newNameTextBox"
                 Margin="0,5,5,5"
                 Grid.Row="1"
                 Grid.Column="1"
                 HorizontalAlignment="Stretch"/>
        <Button x:Name="createButton"
                Grid.Row="2"
                Grid.ColumnSpan="2"
                Margin="50,10"
                Click="CreateButtonClick">Create</Button>
    </Grid>
</Window>
